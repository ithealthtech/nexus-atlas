import {sqliteTable,text,integer,index} from "drizzle-orm/sqlite-core";
export const devices=sqliteTable("devices",{id:text("id").primaryKey(),owner:text("owner").notNull(),tenant:text("tenant").notNull(),name:text("name").notNull(),user:text("user").notNull(),keyId:text("key_id").notNull(),cipher:text("cipher").notNull(),updated:text("updated").notNull(),agentId:text("agent_id"),volumeId:text("volume_id"),protection:text("protection"),collectedAt:text("collected_at")},t=>[index("devices_owner_tenant").on(t.owner,t.tenant)]);
export const shares=sqliteTable("shares",{id:text("id").primaryKey(),owner:text("owner").notNull(),deviceId:text("device_id").notNull(),name:text("name").notNull(),recipient:text("recipient").notNull(),cipher:text("cipher").notNull(),expires:integer("expires").notNull(),used:integer("used").notNull().default(0),revoked:integer("revoked").notNull().default(0)},t=>[index("shares_owner").on(t.owner)]);
export const audit=sqliteTable("audit",{id:text("id").primaryKey(),owner:text("owner").notNull(),action:text("action").notNull(),detail:text("detail").notNull(),time:integer("time").notNull()},t=>[index("audit_owner_time").on(t.owner,t.time)]);

export const agents=sqliteTable("agents",{id:text("id").primaryKey(),owner:text("owner").notNull(),tenant:text("tenant").notNull(),name:text("name").notNull(),tokenHash:text("token_hash").notNull(),publicKey:text("public_key").notNull(),privateKey:text("private_key").notNull(),machineId:text("machine_id"),revoked:integer("revoked").notNull().default(0),lastSeen:text("last_seen"),collectedAt:text("collected_at"),snapshot:text("snapshot"),createdAt:text("created_at").notNull()},t=>[index("agents_owner").on(t.owner),index("agents_token").on(t.tokenHash)]);
export const agentReports=sqliteTable("agent_reports",{id:text("id").primaryKey(),agentId:text("agent_id").notNull(),receivedAt:text("received_at").notNull()},t=>[index("reports_agent").on(t.agentId)]);

